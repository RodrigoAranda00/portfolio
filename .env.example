# Base URL used for metadata (Open Graph / social share image resolution).
# Set this in Vercel to your real deployed domain once the site is live.
# Falls back to http://localhost:3000 if unset — see app/[locale]/layout.tsx.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
